-- This script was generated by the ERD tool in pgAdmin 4.
-- Please log an issue at https://github.com/pgadmin-org/pgadmin4/issues/new/choose if you find any bugs, including reproduction steps.
BEGIN;


CREATE TABLE IF NOT EXISTS public.users
(
    user_id serial NOT NULL,
    user_name character varying NOT NULL,
    user_password character varying NOT NULL,
    user_mail character varying NOT NULL,
    user_phonenumber character varying NOT NULL,
    user_role boolean NOT NULL,
    PRIMARY KEY (user_id)
);

CREATE TABLE IF NOT EXISTS public.orders
(
    order_id serial NOT NULL,
    user_id serial NOT NULL,
    "partDetails_id" serial NOT NULL,
    carport_height numeric NOT NULL,
    carport_width numeric NOT NULL,
    carport_length numeric NOT NULL,
    date date NOT NULL,
    status boolean NOT NULL,
    PRIMARY KEY (order_id)
);

CREATE TABLE IF NOT EXISTS public.parts
(
    part_id serial NOT NULL,
    part_name character varying NOT NULL,
    part_unit character varying,
    part_description character varying NOT NULL,
    PRIMARY KEY (part_id)
);

CREATE TABLE IF NOT EXISTS public."partDetails"
(
    "partDetails_id" serial NOT NULL,
    part_id serial NOT NULL,
    part_amount integer NOT NULL,
    part_length integer NOT NULL,
    PRIMARY KEY ("partDetails_id")
);

ALTER TABLE IF EXISTS public.orders
    ADD FOREIGN KEY (user_id)
    REFERENCES public.users (user_id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION
    NOT VALID;


ALTER TABLE IF EXISTS public.orders
    ADD FOREIGN KEY ("partDetails_id")
    REFERENCES public."partDetails" ("partDetails_id") MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION
    NOT VALID;


ALTER TABLE IF EXISTS public."partDetails"
    ADD FOREIGN KEY (part_id)
    REFERENCES public.parts (part_id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION
    NOT VALID;

END;